package blocksAndDocsLibrary;

/*
https://stackoverflow.com/questions/5456027/how-to-extract-formatting-information-of-word-document-using-apache-poi
https://stackoverflow.com/questions/7102511/how-read-doc-or-docx-file-in-java
*/


public class TextDocHandler {
	
	/*
	 * first need to parse the symbols in a doc/docx
	 * then need to write formula to filter it into blocks
	 * then need to partition blocks into bits
	 * then need to filter text block
	 */
	
	/*
	 * The filtered versions should be stored as transformations from the raw file.
	 * documents should be broken into blocks and advantages first
	 * blocks should be tagged, and then partitioned into title, author, source, and text
	 * filtered blocks should be stored as the original block with a chain of transformations done to it
	 */
	
	
	
	
}
